# 午餐随机选择器：后续对话接手说明

状态日期：2026-09-07

## 1. 阅读顺序与权威关系

新对话开始后按以下顺序恢复上下文：

1. 完整阅读 [SPEC.md](SPEC.md)。它是已经由用户确认的最新产品契约，也是需求冲突时的唯一权威来源；
2. 阅读本文，区分目标契约与当前代码的实现差距；
3. 阅读 [README.md](README.md) 获取当前运行和测试命令；
4. 在做任何修改前重新执行 `git status --short --branch`，检查并保留工作区中的既有改动。

本文只负责交接状态，不重新定义产品规则。若本文与 `SPEC.md` 冲突，以 `SPEC.md` 为准。

## 2. 已完成的契约、实现与验证

- 用户在 2026-08-30 确认了第二轮产品边界，并于 2026-09-01 继续确认一次性 v1 → v2 迁移、三层展示分工、LG 目录优先级、反馈暂缓和推荐菜不展示；
- 新路径中的首个 Codex 对话已重新核对文档、Git 状态、核心代码、餐厅数据和测试，确认交接成功；产品契约已提交为 `2dced2e Update product contract and handoff`；
- 当前工作区随后实现了 v2 个人状态、共享目录／个人叠加层合成、有效 v1 迁移、单一个人备份、可选但不展示的推荐菜、无地图入口，以及 `core.mjs`／`app.mjs`／`view.mjs` 三层边界；
- `view.mjs` 只通过集中校验的 `dispatch(intent)` 与 controller 通信，接收不含持久化字段名的普通 view model；它不导入领域层，也不读写 `localStorage`；
- 2026-09-07 接管后保留并审查全部未提交实现，修复地图来源被当成公开资料入口、合法跳过记录缺少原因时展示失败、保存范围失败后单选框状态不一致，以及手工记录允许未来日期的问题；
- controller 通过 `createController(sharedDocument, initialState, options)` 提供 `dispatch`／`getViewModel`，可注入存储、日期和随机函数用于实际行为验证；`loadStoredState` 覆盖迁移写入边界；
- 当前 `node --test` 实际为 27 项通过，新增迁移写入成功／失败、备份校验／写入失败、目录更新保留个人数据和 controller 抽取确认测试；JavaScript 语法检查和 `git diff --check` 通过；
- 旧对话的浏览器冒烟仅作为历史记录，不能替代本轮验收。本轮独立临时 Chrome 在启动检查阶段出现 `Target crashed`；Browser use 打开本地页面被自动审批拒绝，正在等待用户授予该连接器访问测试页面及测试数据的权限。窄／宽屏和文件选择器尚未通过本轮验收；
- 用户已授权沿契约持续本地实现、验证和按可靠切片提交；未授权推送、部署、问卷或其他远端写入。

## 3. 当前实现与目标契约的已知差距

1. **手工记录与待入库流程仍未实现。** 当前记录对话框仍只能选择有效目录中的正式餐厅；尚无自由文本、别名候选、未关联名称快照、保存后待入库、批量名称预览和去重入口。
2. **个人目录管理尚无网页入口。** schema、有效目录合成和备份已经支持个人新增、共享隐藏及待入库名称，但普通用户目前只能通过手工制作并导入备份来改变这些字段。
3. **忌口功能只有数据与展示基础。** v2 已有 `avoid_pork`，controller 会在偏好为真时为已知提示或未核实信息生成次要提醒，结果卡和目录可以按需展示；但页面尚无偏好设置入口，因此正常流程无法开启或关闭该偏好。
4. **共享目录完成首批修正，完整性仍未确认。** 已补入陈香贵 LG01A、修正肯德基 LG01B，并补充现有 LG 门店的具体来源；当前29家，主范围21家、副范围8家，LG已收录7家。详见 [本轮 LG 核查](data/lg-review-2026-09-07.md)。大众点评搜索页读取失败，尚未取得当前 LG 楼层图及食集档口全集；已请求用户提供导览或名单线索。不能把本轮部分核查当成完整目录，也未刷新全目录旧核验日期。
5. **尚无餐厅 PR 自动校验。** 仓库当前没有对 JSON、唯一 ID、最低字段和副范围步行时间执行 GitHub Actions 检查。
6. **最终视觉验收尚未完成。** 当前只做了默认浏览器宽度下的功能冒烟，尚未检查手机窄屏、桌面宽屏和文件导入选择器；也尚未委派独立前端 Agent 创建 worktree、分支、提交或 PR。

## 4. 已确认的迁移与外部边界

- 第一版暂缓反馈功能，不创建或接入腾讯问卷，不展示反馈入口，也不引入反馈后端；反馈不再是第一版验收或外部依赖。
- 下一版个人状态使用 `lunch-picker.state.v2`。仅在没有有效 v2 且存在有效 v1 时执行一次迁移；成功后以 v2 读写，但不自动覆盖或删除 v1。
- 迁移保留设置、历史和名称快照；旧默认 28 个 ID 不转成个人新增，额外 ID 转为个人新增，旧共享字段覆盖不保留；失败时不得静默创建空状态。
- LG 核查以大众点评作为主要候选发现来源，并使用商场楼层信息、地图或品牌资料交叉核验；必须逐店形成纳入或排除结论，不能把平台搜索排序当作完整性证明。

推荐菜和别名边界：`recommended_dishes` 可选但不展示；`aliases` 为可选字符串数组，只做简单规范化和候选匹配，任何正式关联都必须由用户明确选择。

## 5. 推荐的后续实施顺序

数据契约、共享／个人持久化边界和三层 controller／view 接口已经在当前工作区完成。后续关键路径建议为：

1. 当前 v2／三层实现已通过领域与 controller 测试，可作为独立本地切片提交；浏览器验收缺口须继续补齐，避免与餐厅数据变更混在同一提交；
2. schema 已稳定，按最高优先级完成 LG 候选全集核查和目录更新，再扩充其他楼层与可核验的副范围；
3. 扩展现有集中式 intent 契约，实现手工自由文本、别名候选、待入库、批量名称、个人新增／隐藏和忌口设置闭环；
4. 增加餐厅数据 PR 的最小 GitHub Actions 校验；
5. 数据与剩余 controller intent 稳定后，再让独立前端 Agent 在 worktree／branch 中做视觉设计，并执行手机窄屏和桌面宽屏验收。

每完成一个连贯改动，只运行最相关的最小验证。当前任务已授权本地提交；不得推送、部署、创建问卷或操作 GitHub 远端状态。
