# 午餐随机选择器：后续对话接手说明

状态日期：2026-09-01

## 1. 阅读顺序与权威关系

新对话开始后按以下顺序恢复上下文：

1. 完整阅读 [SPEC.md](SPEC.md)。它是已经由用户确认的最新产品契约，也是需求冲突时的唯一权威来源；
2. 阅读本文，区分目标契约与当前代码的实现差距；
3. 阅读 [README.md](README.md) 获取当前运行和测试命令；
4. 在做任何修改前重新执行 `git status --short --branch`，检查并保留工作区中的既有改动。

本文只负责交接状态，不重新定义产品规则。若本文与 `SPEC.md` 冲突，以 `SPEC.md` 为准。

## 2. 已完成的契约与只读审计

- 用户在 2026-08-30 确认了第二轮产品边界，并于 2026-09-01 继续确认一次性 v1 → v2 迁移、三层展示分工、LG 目录优先级、反馈暂缓和推荐菜不展示；
- 新路径中的首个 Codex 对话已重新核对文档、当前 Git 状态、核心代码、餐厅数据和测试，确认交接足以恢复上下文；本次文档提交使新契约能够从 Git 历史恢复；
- 本轮只把共识写入文档，没有修改 HTML、CSS、JavaScript、测试、餐厅数据、GitHub Actions、问卷或远端仓库；
- 2026-08-30 写文档前，`main` 与 `origin/main` 同步且工作区干净，旧实现基线提交为 `cd25d27ba1dd`；本次文档提交不包含功能实现；
- 当前旧基线的 `node --test` 实际为 13 项通过；这些测试仍锁定旧 20＋8 目录、旧强制字段和双导出格式，不能作为下一版完成证据；
- 不能把文档完成、旧测试通过或交接成功误报为下一版功能已经实现。

## 3. 当前实现与目标契约的已知差距

以下事实来自 2026-08-30 和 2026-09-01 对当前代码的只读核对：

1. **共享目录更新不会到达已有用户。** `js/app.mjs` 会先读取默认 JSON，但只要 `localStorage` 已有状态，就直接恢复其中整份 `restaurants`；目标契约要求共享默认目录与个人叠加层分离，并自动采用部署新版。
2. **当前备份仍包含整份共享目录。** `js/core.mjs` 的备份结构包含 `restaurants`，页面还提供独立餐厅配置导入导出；目标契约只保留一个不含共享目录的个人备份 JSON。
3. **手工记录只能选择现有餐厅。** 当前记录对话框是下拉列表，不支持自由文本、别名候选、批量名称或待入库队列。
4. **尚无通用忌口偏好。** 当前数据使用 `pork_free_hint`，没有用户偏好模型，也没有“仅相关时提示”的展示规则。
5. **地图仍是当前硬依赖。** 当前校验要求每店具有 `map_query`，结果页和目录页会生成高德地图链接；目标契约要求第一版不提供地图入口，也不要求地图字段。
6. **共享目录仍是旧名单。** `data/restaurants.json` 当前有 28 家餐厅，其中位置明确标为 LG 的只有 6 家；名单采用旧的代表性样本口径，尚未按楼层完成完整性复核，也尚未补入陈香贵。现有 `source_urls` 以 Apple Maps 为主，没有保存大众点评链接，不能据此证明候选全集来自大众点评或 LG 已完整覆盖。
7. **推荐菜仍被旧实现强制并展示。** 当前测试要求每店至少一个 `recommended_dishes`，旧页面也展示该字段；目标契约要求它仅作为不展示的可选字段。
8. **展示与业务编排仍混在 `js/app.mjs`。** `js/core.mjs` 已基本保持纯领域逻辑，但 `js/app.mjs` 同时负责状态、`localStorage`、业务流程、DOM 构造和事件绑定；尚无目标契约中的 `js/view.mjs`。
9. **尚无餐厅 PR 自动校验。** 仓库当前没有对 JSON、唯一 ID、最低字段和副范围步行时间执行 GitHub Actions 检查。
10. **尚未委派前端 Agent。** 已确认新的三层协作边界，但没有创建对应 worktree、分支、提交或 PR。

当前没有图片资产，符合“不做真实图片”的新目标；但页面文案仍包含“以后补充授权照片”等旧表述，实施时应一并删除。

## 4. 已确认的迁移与外部边界

- 第一版暂缓反馈功能，不创建或接入腾讯问卷，不展示反馈入口，也不引入反馈后端；反馈不再是第一版验收或外部依赖。
- 下一版个人状态使用 `lunch-picker.state.v2`。仅在没有有效 v2 且存在有效 v1 时执行一次迁移；成功后以 v2 读写，但不自动覆盖或删除 v1。
- 迁移保留设置、历史和名称快照；旧默认 28 个 ID 不转成个人新增，额外 ID 转为个人新增，旧共享字段覆盖不保留；失败时不得静默创建空状态。
- LG 核查以大众点评作为主要候选发现来源，并使用商场楼层信息、地图或品牌资料交叉核验；必须逐店形成纳入或排除结论，不能把平台搜索排序当作完整性证明。

推荐菜和别名边界：`recommended_dishes` 可选但不展示；`aliases` 为可选字符串数组，只做简单规范化和候选匹配，任何正式关联都必须由用户明确选择。

## 5. 推荐的后续实施顺序

下一次对话只有在用户明确要求实现后才开始改代码。关键路径建议为：

1. 先更新数据契约、共享目录／个人叠加层的持久化边界和核心测试；
2. 将 `js/app.mjs` 收敛为 controller／持久化入口，新建只负责 DOM 和事件的 `js/view.mjs`，并建立最小接口测试；
3. schema 稳定后优先完成 LG 候选全集核查和目录更新，再扩充其他楼层与可核验的副范围；
4. 实现手工自由文本、别名候选、待入库、批量名称、忌口偏好和新版备份；
5. 增加餐厅数据 PR 的最小 GitHub Actions 校验；
6. 数据、持久化和 controller／view 接口稳定后，再让独立前端 Agent 在 worktree／branch 中做界面设计。

每完成一个连贯改动，只运行最相关的最小验证。未经用户明确授权，不创建提交、不推送、不部署、不创建问卷，也不操作 GitHub 远端状态。
