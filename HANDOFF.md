# 午餐随机选择器：后续对话接手说明

状态日期：2026-09-23

## 1. 阅读顺序与权威关系

新对话开始后按以下顺序恢复上下文：

1. 完整阅读 [SPEC.md](SPEC.md)。它是已经由用户确认的最新产品契约，也是需求冲突时的唯一权威来源；
2. 阅读本文，区分目标契约与当前代码的实现差距；
3. 阅读 [README.md](README.md) 获取当前运行和测试命令；
4. 在做任何修改前重新执行 `git status --short --branch`，检查并保留工作区中的既有改动。

本文只负责交接状态，不重新定义产品规则。若本文与 `SPEC.md` 冲突，以 `SPEC.md` 为准。

## 2. 已完成的契约、实现与验证

- 用户在 2026-08-30 确认了第二轮产品边界，并于 2026-09-01 继续确认一次性 v1 → v2 迁移、三层展示分工、LG 目录优先级、反馈暂缓和推荐菜不展示；
- 新路径中的首个 Codex 对话已重新核对文档、Git 状态、核心代码、餐厅数据和测试，确认交接成功；产品契约已提交为 `2dced2e Update product contract and handoff`；
- 当前工作区随后实现了 v2 个人状态、共享目录／个人叠加层合成、有效 v1 迁移、单一个人备份、可选但不展示的推荐菜、无地图入口，以及 `core.mjs`／`app.mjs`／`view.mjs` 三层边界；
- `view.mjs` 只通过集中校验的 `dispatch(intent)` 与 controller 通信，接收不含持久化字段名的普通 view model；它不导入领域层，也不读写 `localStorage`；
- 2026-09-07 接管后保留并审查全部未提交实现，修复地图来源被当成公开资料入口、合法跳过记录缺少原因时展示失败、保存范围失败后单选框状态不一致，以及手工记录允许未来日期的问题；
- controller 通过 `createController(sharedDocument, initialState, options)` 提供 `dispatch`／`getViewModel`，可注入存储、日期和随机函数用于实际行为验证；`loadStoredState` 覆盖迁移写入边界；
- 当前 `node --test` 实际为36项通过（2026-09-22重新运行），覆盖迁移与备份写入失败、自由文字／别名候选、待入库预览和明确关联、个人新增／隐藏恢复、忌口展示与最低数据字段；JavaScript 语法检查和 `git diff --check` 通过；
- 手工记录现在允许任意非空文本，仅显式选择候选才关联正式餐厅；保存未关联记录后可选择加入本地待入库。批量名称先拆分、清理空白、精确去重并展示预览，再确认保存；共享目录更新只产生候选，不会自动改写历史；
- 数据页提供个人正式新增、隐藏／恢复和“不吃猪肉”偏好。隐藏仅影响随机池与有效目录，仍可手动记录这家餐厅；待入库不参与随机，忌口不改变候选权重；
- 餐厅PR校验由 `.github/workflows/validate-restaurants.yml` 调用 `scripts/validate-restaurants.mjs`，仅授予仓库读取权限。2026-09-08 本地脚本验证29家目录通过、非JSON输入按预期失败；当时仅完成本地验证；2026-09-22重新校验48条目录通过，本轮没有触发餐厅PR工作流（只在`pull_request`事件运行）；
- 2026-09-08 用户确认只使用内置浏览器继续自动化，不使用外部 Chrome 或原生电脑控制；此前连接器审批阻塞已解除，不应重复请求授权。实际发现 `window.confirm` 会等待人工操作，现已统一改为网页内确认对话框，保留取消／Escape、覆盖警告和确认语义；
- 本轮内置浏览器已实际验证三种范围、换一家不重复、确认写入、自由文字／候选关联／改名取消关联、待入库预览与明确关联、个人新增／隐藏恢复、忌口和刷新保留；备份无效文件拒绝、取消不改数据及完整个人数据恢复通过。365×812与1280×900布局已检查，修复窄屏自动滚动时底部导航遮挡确认按钮，并更新样式引用以加载修正版。页面控制台没有捕获到错误或警告，测试后已恢复空白个人状态和默认视口；
- 后续复核发现先前365／1280两档未覆盖的中等宽度标题挤压，现将首页分栏阈值移至1024像素，并修正320像素横向溢出与按钮换行、装饰圆环遮挡忌口提醒、恢复备份后遗留旧抽签提示。已检查320、365、759、760、828、1023、1024及1280像素宽度；在真实页面验证副范围8家整轮不重复与耗尽提示，以及抽签后导入恢复清空提示。复核结束恢复空白个人状态与默认视口；
- 已补齐实际备份下载与恢复闭环：新内置标签页导出完整测试备份，核对实际下载文件后导入空白状态，刷新并再次导出；除正常变化的 exported_at 外，全部个人字段一致，最后恢复空白测试状态。Blob 下载未被工具事件报告，不能仅凭事件判断失败；旧标签页未产生文件、新标签页成功的底层原因仍未确定，没有为此改动导出代码；
- 2026-09-22用户明确授权盘点状态、更新必要文档、分批提交并推送到现有远端仓库；旧“不得推送”限制已被本次授权替代。本次不改变托管配置，也不创建问卷或执行其他远端业务写入。

## 3. 当前实现与目标契约的已知差距

1. **用户接受当前目录，完整性不作为本轮发布前提。** 当前48条记录，有效38家（主范围32、副范围6），其中有效LG20家、停用10条。2026-09-09最新一批用本店关联团购条款补齐堂食／单人正餐依据，新增嗲团团和Tuna maki；旧活动名单的彼得家、河仙原链接、鳗享屋、钱屋、魔王猪蹄已核实不应纳入。2026-09-23用户接受当前目录，不再追查完整名册；潮馐美和甬府小鲜按用户决定不收录并关闭补查。两家原本均未进入共享JSON，不需要删除数据，也不据此推断闭店。此前点评两分类1–26页和楼层相册9页134张已经检查，不重复其检索；不能把搜索末页当作全集证明。逐店证据及历史修正见 [目录核查](data/lg-review-2026-09-07.md)。2026-09-10完成保留6家副范围的本店资料与固定泰康保险大厦起点步行导航核验：草根5、老乡鸡7、鹅匠4、欧记4、阿祥嫂5、张杨路裕兴记6分钟。老乡鸡改为1F，欧记明确2F铺位。永和旧址已换为阿祥嫂；裕兴记新大陆原址未能核实，停用但不宣称闭店，张杨路现店用独立ID新增。付小姐和西贝明确闭店。未刷新全目录核验日期。本批数据验证结果见目录核查末尾；此前页面功能验证为历史结果，本批未重做浏览器功能测试。
2. **视觉改版已审查，剩余交互改为发布后测试。** 原始交付记录见第7节，PR复审的内置浏览器补验结果见第8节。2026-09-23用户明确决定先发布到GitHub Pages，再自行在线测试；真实键盘、原生文件选择器、减少动态偏好仍为未验证，不再阻塞本轮发布。最新决定与公共目录维护方向见第9节。

## 4. 已确认的迁移与外部边界

- 第一版暂缓反馈功能，不创建或接入腾讯问卷，不展示反馈入口，也不引入反馈后端；反馈不再是第一版验收或外部依赖。
- 下一版个人状态使用 `lunch-picker.state.v2`。仅在没有有效 v2 且存在有效 v1 时执行一次迁移；成功后以 v2 读写，但不自动覆盖或删除 v1。
- 迁移保留设置、历史和名称快照；旧默认 28 个 ID 不转成个人新增，额外 ID 转为个人新增，旧共享字段覆盖不保留；失败时不得静默创建空状态。
- LG 核查以大众点评作为主要候选发现来源，并使用商场楼层信息、地图或品牌资料交叉核验；必须逐店形成纳入或排除结论，不能把平台搜索排序当作完整性证明。

推荐菜和别名边界：`recommended_dishes` 可选但不展示；`aliases` 为可选字符串数组，只做简单规范化和候选匹配，任何正式关联都必须由用户明确选择。

## 5. 原计划完成情况与后续顺序

2026-09-22交接前盘点（历史状态，本轮接手后的变化见第7节）：

- **功能迁移与业务闭环已完成。** v2共享／个人数据分离、v1迁移、完整备份、手工记录、待入库、个人增删叠加、忌口以及三层接口均已实现；现有浏览器交互与响应式修复也已提交。
- **目录核查部分完成。** 已补陈香贵并扩充LG和其他楼层，现有有效38家、停用10条，保留6家副范围路线已于9月10日核验。潮馐美现状、甬府本店午餐适用条件与当前完整商场名册尚缺证据；9月22日只核对仓库事实，没有重新联网核验餐厅营业状态。
- **独立视觉设计尚未开始。** 当前已有可用界面，后续独立视觉方案、对应worktree／分支／PR仍未启动；不与已完成的功能界面混为一谈。
- **本地验证已刷新。** 首次全量测试35通过、1失败：抽签边界断言仍期待已停用的付小姐。`f7cd213`改为固定测试样例，覆盖首尾停用店、范围排除及0／0.5附近的等权边界；未改抽签算法。随后36项全部通过，48条目录校验通过。浏览器验证仍为9月8日的历史结果，本轮未重跑。
- **提交和发布分开记述。** 本轮开始时工作区干净，刷新远端后确认`main`领先`origin/main`18个提交、落后0个；历史提交已按契约、功能、交互修复和目录核查拆分，无需改写。本次追加测试修正和文档同步两个提交，随后按授权推送。GitHub Pages管理接口本轮返回404，未确认当前托管配置或部署结果；历史文档记录发布源为`main`根目录，推送可能触发现有自动发布。以实际远端分支和部署状态为准，不把本地测试等同线上验收。

数据契约、共享／个人持久化边界、记录和个人管理入口及三层 controller／view 接口已完成本地实现。2026-09-23更新后的后续顺序为：

1. 将已审查的视觉改版与测试修复经现有PR发布到GitHub Pages，由用户直接在线试用；
2. 后续确定便于用户与同事共同维护公共餐厅目录的简单方式，例如腾讯文档或仓库CSV；本次不实施数据源迁移，不继续重复完整名册与两家候选的检索。

每完成一个连贯改动，只运行最相关的最小验证。此前只推送独立分支、暂不发布的限制，已被2026-09-23用户要求直接推送并在GitHub Pages测试的决定替代；沿用现有PR和Pages流程，不调整部署配置。

## 6. 界面协作边界与交接前验证记录

- 功能接口基线为本地提交 `b41ead2`，随后已完成网页内确认、响应式布局与抽签提示同步修正；36项测试通过。接手时读取实时状态和最新提交，不把历史接口基线当作当前界面代码。
- 产品规则仍完整读取 `SPEC.md`。前端实现范围为 `index.html`、`styles.css`、`js/view.mjs` 及独立 UI 测试；不要修改 `js/core.mjs`、`js/app.mjs`、餐厅事实数据或持久化测试。
- `view.mjs` 顶部集中定义 `VIEW_INTENTS`、`INTENT_PAYLOAD_KEYS`、`createViewIntent` 和 `validateViewModel`；`createView` 内集中缓存页面 ID。界面从 controller 接收普通 view model，只发送已定义的用户意图。
- 当前交互包括抽签／确认、自由文字记录与显式候选、备份确认、单个／批量待入库、待入库关联确认、个人新增／隐藏恢复和忌口开关。`tests/core.test.mjs` 保护接口与选择器，`tests/controller.test.mjs` 和 `tests/personal.test.mjs` 保护状态行为。
- 可以新增独立 `tests/view.test.mjs` 检查 DOM 行为；不得为通过测试删改领域、持久化或接口断言。若新界面要求更改 intent、view model、随机或存储语义，先返回用户决策。
- 实际启动前重读 Git 状态，从本地已审查提交创建独立 `codex/` 分支和 worktree；不要与主工作区共用未提交修改。交接前只完成接口准备；2026-09-22后续接手已启动独立视觉实现，见第7节。
- 视觉验收使用明确标记的测试数据，覆盖窄／宽屏、键盘操作、修改名称清除关联、未关联保存后待入库、批量预览、备份取消／恢复及隐藏恢复。本轮这些主要流程已有实际浏览器证据；后续改动按影响范围复核。
- 内置浏览器文件选择自动化：先等待 `filechooser` 事件，同时发起点击而不先等待点击结束；取得 chooser 后调用 `setFiles`，最后等待点击完成。本轮实测此顺序可自行完成导入，先等待点击结束会卡在文件窗口。所有确认均通过网页内按钮操作，不调用原生电脑控制。

## 7. 2026-09-22：独立分支接手与本轮边界

- **基线已核实。** 主代理完整阅读SPEC、本文、README及目录核查记录；仓库和适用父目录没有AGENTS.md。本地初始工作区干净，`main`与`origin/main`均为`9d0a7a075071d06fa0fc6918fc2b8aeab2aa84fe`，未回退或改写远端后续内容。基线36项测试、48条目录校验及差异检查通过；直接统计有效38家、主范围32、副范围6、有效LG20家、停用10条。
- **目录补查已收敛。** 三项缺口经有限公开搜索与新来源读取仍未解决，已追加到[核查记录](data/lg-review-2026-09-07.md#2026-09-22三个剩余缺口有界补查)。没有重做已完成的点评分页／相册搜索，没有修改餐厅数据或全目录核验日期。
- **视觉实现已提交。** 暖纸色、墨色与柿橙午餐签卡；桌面将范围／抽签和结果并排，移动端单列，空白签卡与结果原位切换，统一历史、数据、说明及目录样式。导航在宽屏位于页头，窄屏保留底部导航；补强单选项／导入入口的键盘焦点、44px操作目标、长文本换行和减少动态偏好。只修改`index.html`、`styles.css`和`js/view.mjs`；展示层新增的空态由`currentRestaurant`派生，不改intent、view model、领域规则、持久化或餐厅数据，没有新增依赖。
- **本轮代码验证。** 视觉改版后36项`node --test`、48条`node scripts/validate-restaurants.mjs`、展示层语法及`git diff --check`通过。静态HTML标签结构与颜色对比度另行检查；主按钮文字对比度约5.06:1，次要文字在较深纸色背景约4.65:1。数值和结构检查不代表实际浏览器布局验证。
- **浏览器验证受限。** Cloud Browser打开本地HTTP预览返回`net::ERR_BLOCKED_BY_CLIENT`；直接文件URL又被浏览器安全策略拒绝。已停止这条验证路径，没有绕过限制或发布网站来验证。新视觉的实际窄／宽屏、键盘交互、自由文本关联、备份取消／恢复与下载闭环均为**本轮未运行**，后续在可用的内置浏览器环境补验。现有Node测试通过不能替代这项验收。
- **审查与后续。** 主代理及独立只读审查代理均核对了视觉差异，未发现具体需返修问题；独立审查也重新运行36项测试与48条校验通过。交付采用独立分支的草稿PR，保留浏览器验收缺口供审查，不合并或主动发布。下一步在可用的内置浏览器环境复核320／365px、导航切换断点959／960px及宽屏，覆盖长店名、结果与空态、键盘焦点、自由文本改名取消关联、待入库与个人隐藏恢复、备份无效文件／取消／恢复／实际下载；再根据用户对视觉的意见调整。目录部分只在出现新的有效来源时继续三项缺口，不循环旧搜索。

## 8. 2026-09-22：PR#1复审

- **审查范围与判断。** 对照实时PR元数据，基线为`9d0a7a0`，待审头为`d370a61`。暖纸色签卡沿用原生静态页面和既有controller接口，符合独立视觉任务，无需设计重写；没有发现本PR新增的业务或安全缺陷。目录补查明确保留三个证据缺口，餐厅JSON未变。
- **修复测试误报。** 本次Windows工作树使用CRLF，首次`node --test`为35通过、1失败：元素缓存检查只匹配LF，无法定位列表。该问题在基线已存在；仅让`tests/core.test.mjs`中的列表定位正则兼容CRLF，保留ID唯一性和全部缓存ID存在性断言。修复后36项通过，48条餐厅校验、展示层语法和差异检查通过。
- **本次实际浏览器检查。** 使用内置浏览器访问绑定`127.0.0.1`的本地HTTP预览，无需发布。专用测试端口的同源iframe分别使用320、365、520、639、640、759、959、960、1024、1280px宽度，检查四个页面无横向溢出、导航959／960断点、空态／结果互斥、抽签后确认按钮不被底部导航遮挡和记录弹窗无横向溢出，均通过；该方法验证CSS视口，不声称覆盖手机硬件或所有浏览器。
- **主要交互已通过。** 三种范围、换一家不重复、副范围耗尽提示、确认前不写记录、候选显式关联／改名取消关联、自由文本保存与待入库、批量预览去重、关联取消／确认、个人新增和隐藏恢复、长名称换行、忌口及刷新保留。HTML形状的测试名称只作为文字呈现，没有生成元素。使用专用端口的合成测试数据，结束后恢复进入测试前的个人状态。
- **备份验证分层记录。** 浏览器DOM文件注入覆盖无效JSON不改数据、有效备份取消不改数据、空白恢复、完整个人数据恢复及刷新保留；另从真实导出按钮取得实际下载文件，运行`validateBackupDocument`并与空白个人状态逐字段比较通过。没有把“备份已生成”提示或工具事件当作下载证据，也没有把DOM文件注入称为原生文件选择器验收。
- **留给作者的验收。** 当前工具按键标记为`synthetic`，只能确认导航激活和Tab后活动元素，不能据此确认真实键盘焦点样式、Escape／弹窗焦点闭环；原生文件选择器、系统减少动态偏好及真机触控仍未覆盖。建议这些交互补验后再解除草稿并发布；Pages部署状态仍未核实。三项餐厅证据缺口继续等待新的有效来源，不据本轮代码验证更新现实营业结论。
- **复现材料。** 本地临时验收页及脚本保存在`C:/AgentTemp/lunch-picker-pr1-review-20260922/`，不进入仓库或部署产物。此次仅修改测试定位正则及验证文档，没有修改运行时代码、数据、部署配置或依赖。

## 9. 2026-09-23：目录范围收敛与在线测试决定

- 用户接受当前目录，不要求自己确认或提供完整商场／LG名册；不再把目录完整性作为本轮发布门槛，仍不宣称已覆盖全部门店。
- 潮馐美、甬府小鲜按用户决定不收录，关闭此前两项待补查。实际读取共享JSON确认两者原本均不存在，48条记录／有效38家（主32、副6）保持不变；没有删除其他餐厅、改写历史证据或将资料不足标成闭店。
- 后续需要便于用户和同事共同维护的公共餐厅目录，腾讯文档或仓库CSV都是候选；具体方案尚未选择，本次不新增数据库或同步流程，也不形成双份正式数据源。
- 用户暂不进行本地人工验收，授权先推送发布，再直接在GitHub Pages测试。第8节尚未覆盖的真实键盘、原生文件选择器及减少动态偏好保留为未验证，转为上线后的试用事项。
- 发布前已确认现有Pages构建和部署来自`main`，最近成功部署为`9d0a7a0`（Actions运行`35691212569`）。因此采用更新PR#1、合入`main`并观察既有Pages发布的路径，不更改部署配置；最终结果以对应新提交的运行和站点内容为准。
