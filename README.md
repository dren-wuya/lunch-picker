# 午餐随机选择器

> 开发状态（2026-09-23）：午餐签卡视觉改版及PR复审已完成，本地36项测试、48条目录校验通过。用户接受现有38家有效目录，决定不收录潮馐美和甬府小鲜，并授权先发布到GitHub Pages再在线测试。真实键盘、原生文件选择器和减少动态偏好仍未完成验证，但不再阻塞本轮发布。便于同事维护的公共目录入口列为后续工作。产品边界见 [SPEC.md](SPEC.md)，验证和决定记录见 [HANDOFF.md](HANDOFF.md)。

一个面向上海陆家嘴中心 L+MALL 泰康大厦工作日午餐的手机优先静态网页。第一版不需要账号、后端或构建步骤，所有历史和设置都保存在当前浏览器中。

当前“说明”页公开随机机制、目录边界和共享目录与个人叠加层合成后的有效餐厅目录。页面不展示图片、地图或推荐菜；有来源时仅提供公开资料入口。DOM 展示与事件已集中在 `js/view.mjs`，业务 controller 和持久化位于 `js/app.mjs`，纯规则与数据契约位于 `js/core.mjs`。第一版暂缓反馈功能。

已确认的完整产品边界见 [SPEC.md](SPEC.md)。

## 本地运行

浏览器不能稳定地从 `file://` 地址读取默认 JSON，因此请在仓库目录启动任意静态文件服务器。例如：

```powershell
python -m http.server 8000
```

然后访问 `http://localhost:8000/`。

## 验证

项目没有第三方运行时依赖。随机与数据规则使用 Node.js 内置测试运行器验证：

```powershell
node --test
node scripts/validate-restaurants.mjs
```

当前测试共36项，包含领域规则、controller 意图、迁移与备份失败保留原数据、目录更新后的个人数据保留，以及新增记录和个人管理流程。2026-09-22视觉改版后重新运行全部通过；交接前已将依赖停用门店的抽签边界断言改为固定样例，本次未修改领域规则、controller或持久化实现。

本轮视觉改版使用暖纸色签卡、桌面双栏与移动端单列，统一导航、记录、数据和目录样式，补充键盘焦点与减少动态支持。最初Cloud Browser预览受限；2026-09-22 PR复审使用可访问本地HTTP的内置浏览器补验：在同源iframe中检查320、365、520、639、640、759、959、960、1024及1280px布局、空态／结果切换和确认按钮遮挡，主要记录与个人数据流程均通过。导入测试通过DOM文件注入覆盖无效文件、取消、完整个人数据恢复和刷新保留；另已核对实际下载的空白个人备份。工具按键为合成事件，**真实键盘焦点／Escape、原生文件选择器及系统减少动态偏好仍未完成验收**；按2026-09-23用户决定，转为发布后的在线测试项。iframe检查不等于真机测试。详见 [PR复审记录](HANDOFF.md#8-2026-09-22pr1复审)。

历史验证：2026-09-08使用内置浏览器完成抽签、自由文字与显式关联、待入库、个人新增／隐藏恢复、忌口、备份无效文件／取消／恢复和刷新保留检查，并检查320、365、759、760、828、1023、1024及1280像素宽度。确认操作使用网页内对话框，保留取消与确认；当时修复了中等宽度标题挤压、320像素溢出、底部导航遮挡确认按钮、装饰遮挡忌口提醒和恢复备份遗留提示的问题。测试后恢复空白个人状态和默认视口。这些记录不构成本轮新视觉的验证。

9月8日的实际备份下载与恢复闭环曾在内置浏览器通过：读取完整个人备份的实际下载文件，导入空白状态，刷新并再次导出；除正常变化的导出时间外，所有个人字段一致。Blob下载未必触发工具的download事件，验证以实际文件和恢复结果为准；本轮未重复此项。

手工记录中输入名称后，只有点击候选才会关联正式餐厅；直接保存会保留输入原文，并可随后加入本地待入库。数据页可批量整理名称、维护个人随机池及设置忌口。共享目录更新不会自动改写既有记录，待入库名称始终不参与随机。

餐厅数据PR通过GitHub Actions运行同一个校验脚本，检查JSON可解析、唯一ID、名称／范围／文字位置和副范围1–8分钟步行时间，并复用运行时对可选字段的格式校验。检查通过不代表现实事实已经核实，仍需人工审核范围、营业与午间适用性；工作流不自动合并或部署。该工作流仅由PR触发，直接推送main不会运行这项校验。2026-09-22本地校验48条目录通过；不据此声称已通过线上Actions。

## 当前实现的数据

- 共享默认目录位于 `data/restaurants.json`；当前48条记录，有效目录为32家主范围和6家副范围餐厅，其中LG20家，另保留10条停用记录。每次启动都读取部署版目录，再叠加当前浏览器的个人新增与隐藏项。2026-09-10完成保留6家副范围的本店资料和固定泰康保险大厦起点步行导航核验，修正地址并用独立ID收录阿祥嫂和张杨路裕兴记；旧永和、原址未能核实的裕兴记以及明确闭店的付小姐和西贝停用；2026-09-23用户接受当前目录，不再要求补齐完整名册；潮馐美和甬府小鲜原本均未入库，按用户决定关闭补查并保持不收录，目录数量不变，见 [目录核查](data/lg-review-2026-09-07.md)。
- 参考人均和副范围步行时间是公开信息基础上的近似值，不是实时承诺；默认列表核验日期为 2026-08-27。
- 后续拟提供便于用户和同事共同维护的公共目录入口，腾讯文档或仓库CSV均为候选，具体方案另行确定；本次仍以 `data/restaurants.json` 为唯一共享数据源。
- 当前数据可以保留 `recommended_dishes`，但该字段可缺失，页面不展示，也不把它作为入库条件。
- 网页只导入导出一个 `lunch-picker-backup.json` 个人备份；它包含设置、忌口偏好、个人叠加层、待入库名称和历史，不复制或覆盖共享目录。
- 新状态写入 `lunch-picker.state.v2`。没有有效 v2 但存在有效 v1 时会迁移设置、历史和个人额外餐厅；旧 `lunch-picker.state.v1` 不会自动删除。
- 个人历史只存在浏览器本地；不要把个人导出文件提交到公开仓库。

## GitHub Pages

代码只使用相对路径。2026-09-23从Actions和部署记录确认，现有GitHub Pages流程从 `main` 发布；Pages管理接口仍返回404，不据此推断站点未启用。用户已授权更新并合入本次PR，沿用既有Pages流程发布后在线测试，不调整部署配置。发布是否成功以对应提交的Pages任务与实际站点内容为准，本地测试不等于线上交互验收。

- Repository: https://github.com/dren-wuya/lunch-picker
- Pages: https://dren-wuya.github.io/lunch-picker/
