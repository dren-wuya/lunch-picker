# 午餐随机选择器：产品与实现契约

状态：已确认

确认日期：2026-09-01

最近修订：2026-09-23（接受当前目录、排除未核实候选、公共维护方向与发布后测试）

本文档记录第一版已经确认的产品边界与验收口径。第一版以可用、透明、易维护为目标，不扩展为通用餐饮推荐平台。

本文档是产品契约的唯一权威来源。它描述下一版应达到的目标，不表示当前代码已经实现；当前实现差距和新对话的接手顺序见 [HANDOFF.md](HANDOFF.md)。

## 1. 目标

为上海陆家嘴 L+MALL 泰康大厦的工作日午餐提供一个手机优先的随机选择工具，减少每天决定吃什么的成本。

典型使用时间为 11:30–13:00，默认服务 1–2 人的普通工作日午餐。

## 2. 餐厅范围

### 2.1 主范围

位于上海陆家嘴中心 L+MALL 内、从泰康大厦可以不离开综合体到达的全部合格餐饮门店。

### 2.2 副范围

从泰康大厦办公楼大堂出发，地图步行导航单程不超过 8 分钟、无需乘坐交通工具，且不属于主范围的合格餐饮门店。

### 2.3 入选条件

餐厅必须同时满足：

- 可以堂食，并能让一人点到一份完整正餐；
- 适合 1–2 人普通工作日午餐；
- 工作日午间通常无需预约；
- 到店后通常可在 40 分钟内完成用餐；
- 位置、午间可用性和参考价格可以从公开来源合理核验。

纳入正餐餐厅、快餐、面馆、米饭套餐、轻食和美食广场档口。排除纯饮品店、甜品店、烘焙店、便利店、仅售小吃的门店和纯外卖档口。

正常午餐人均 100 元作为收录时的软上限。少量超过 100 元但仍适合单人午餐的门店可以入选；价格不影响抽取权重。

### 2.4 忌口边界

- “不吃猪肉”不再是餐厅入选硬条件，而是通用“忌口偏好”中的一个选项；
- 第一版只迁移和维护已经存在的“不吃猪肉”提示，不虚构素食、过敏原等尚未核实的数据；
- 忌口偏好不筛除餐厅，也不改变抽取权重；
- 只有用户选择了某项忌口，并且当前餐厅存在相关提示或信息尚未核实时，结果卡片底部才显示次要提醒；
- 未选择忌口或没有相关信息时，不在主界面占用固定位置；提示不能替代用户到店点餐时确认配料。

## 3. 共享默认目录

- 2026-09-23 用户接受当前38家有效餐厅目录，完整性未证明不再作为本轮发布前提；后续按新增可靠线索逐步维护，不要求用户补齐完整商场名册；
- 大众点评作为发现候选门店的主要来源，L+MALL 商户信息或楼层图用于核对铺位范围，高德地图、Apple Maps、品牌官网等公开来源用于交叉核验位置、营业状态和午间适用性；
- 仍不宣称目录完整。未来如需作完整性判断，应取得可逐店对照的当前名册，不能用平台搜索排序或末页代替；无需为本次发布继续重复检索；
- 后续主范围扩充可按楼层核查，收录符合第 2 节条件且有依据的门店，不以固定家数或菜系配额为目标；
- 潮馐美、甬府小鲜因证据不足且用户决定不收录，从当前待补查事项中关闭；这不构成闭店或不提供午餐的事实判断。两家原本均不在共享目录中，不新增停用占位记录；
- 副范围按“泰康大厦办公楼大堂步行导航单程不超过 8 分钟”逐店核实，可以分批扩充，但不得为了凑数放宽边界；
- 覆盖中餐、面饭、日料、韩餐、西式、轻食和快餐等常见类型，但菜系覆盖本身不是配额；
- 外部平台只用于建立和人工维护白名单，网页运行时不实时请求餐厅 API。

### 3.1 正式记录最低门槛

- 普通用户提交入库线索时只要求餐厅名称，可以附加位置或说明，但不能要求用户填写完整结构化资料；
- 正式共享记录至少具有稳定 `id`、名称、`primary`／`secondary` 范围和可定位的文字位置；稳定 `id` 由仓库维护者或 Agent 生成，不由普通用户填写；可选 `aliases` 使用非空字符串数组保存已维护别名；
- 副范围记录还必须包含经核实的 1–8 分钟步行时间；
- 菜系、参考人均、推荐菜、忌口提示和公开来源均为可选增强字段，不得因这些信息暂缺而阻塞一家具备最低事实的合格餐厅入池；
- `recommended_dishes` 可以继续作为不展示的可选数据字段；第一版页面不读取或展示它，也不把它作为验收或入库条件；
- 第一版不保存图片字段，也不要求地图搜索词、坐标或地图链接；
- 共享默认目录继续维护在单一 `data/restaurants.json` 中。

## 4. 随机规则

### 4.1 范围选择

提供三个选项：

- `楼下`：只抽取主范围，默认选择；
- `出去走走`：只抽取副范围；
- `都可以`：合并主、副范围。

### 4.2 权重与冷却

- 普通候选餐厅权重为 `1.0`；
- 吃过一家餐厅后，接下来的两条有效餐厅午餐记录中，该餐厅处于冷却状态；
- 冷却餐厅的权重由内部配置控制，第一版默认值为 `0.0`；
- 冷却权重不提供用户界面，但代码应允许以后修改；
- `跳过`、`未记录`、周末和其他非餐厅午餐不消耗冷却次数；
- 第一版不增加“最近若干顿最多出现若干次”等滚动频次规则；
- 在当前范围和历史约束确定权重后，按权重随机抽取。第一版中所有非冷却餐厅等概率。

### 4.3 一次抽取会话

- 抽中后显示 `就吃这家` 和 `换一家`；
- 只有点击 `就吃这家` 才写入实际午餐记录；
- 点击 `换一家` 后，从本轮尚未出现的可用餐厅中继续随机；
- 同一轮不会重复出现已经展示过的餐厅；
- 本轮候选耗尽时明确提示，不静默放宽范围或冷却规则。

## 5. 午餐记录

### 5.1 每日模型

- 每个工作日最多一条午餐记录；
- 用户可以补记过去的工作日，也可以修改或替换已有记录；
- 周末自动视为非工作日，不要求用户填写；
- 历史频次根据当前记录重新计算，编辑记录不会重复累计。

### 5.2 状态

- `restaurant`：记录一顿餐厅午餐。可以关联正式餐厅，也可以只保存用户输入的名称快照；只有已关联记录会更新对应餐厅的冷却历史；
- `skipped`：不关联餐厅、不进入冷却计数，可记录请假、公司聚餐在别处或其他原因；
- `unrecorded`：默认状态，表示尚未填写，不据此推断实际午餐。

公司聚餐若去了白名单餐厅，仍记录该餐厅并更新其 `last_seen`。公司聚餐地点不在白名单或不想记录时，使用 `skipped`。

每条餐厅记录应保留餐厅名称快照，避免餐厅以后从当前配置移除后历史无法显示。

### 5.3 手工名称与待入库

- 手工记录允许输入正式店名、简称或任意自由文本，不强迫用户先维护餐厅目录；
- 输入时按正式名称和已维护别名显示候选，只有用户明确选择候选时才关联正式餐厅，不做自动模糊绑定；
- 别名保存前清理首尾空白并在单店内精确去重；候选搜索只做大小写、全半角等简单规范化和包含匹配，不做自动纠错。多个餐厅命中相同名称或别名时同时展示，由用户选择；
- 未选择候选时先按原文保存午餐记录，保存后再由用户决定是否提交入库申请；错别字和一次性地点不能自动污染随机池；
- 单个名称或批量名称均可进入“待入库”。批量输入使用普通文本框，接受中文逗号、英文逗号和换行，确认前展示拆分、清理空白和精确去重后的预览；
- 待入库名称不参与随机。维护者取得线索后，让 Agent 查证并补全正式记录，通过 PR 人工审核合并；
- 共享目录更新后，如待入库名称出现正式名称或别名候选，仍由用户确认关联，不自动改写既有历史。

## 6. 用户界面

### 6.1 首页

- 范围选择：`楼下`、`出去走走`、`都可以`；
- 主操作：开始随机；
- 结果卡片及 `就吃这家`、`换一家`；
- 今天已有记录时显示当前状态；
- 提供手动输入餐厅名称和 `今天跳过` 的入口。

### 6.2 结果卡片

展示：

- 餐厅名称；
- 商场楼层、铺位或街道地址；
- `楼下` 或 `出去走走` 标签；
- 副范围的参考步行时间；
- 已有数据时可展示菜系或正餐类型和参考人均价格；
- 仅在触发第 2.4 节条件时展示次要忌口提示。

结果卡片不搬运评分、评论、菜单图片或宣传照片；第一版不展示真实图片，也不提供地图入口。

### 6.3 历史与数据

- 历史页按日期倒序显示已有记录；
- 支持补记过去工作日和修改已有记录；
- 不做完整工作日日历、统计图表或趋势分析；
- 提供单一完整备份 JSON 的导入导出入口；
- 提供个人隐藏、待入库和批量名称录入入口，但不提供正式共享餐厅字段的网页内编辑器。

### 6.4 说明与餐厅目录

- 公开解释范围过滤、两顿冷却、等概率抽取、换一家和确认记录的完整机制；
- 明确价格、菜系和距离不参与抽取权重；
- 展示共享默认目录与个人叠加层合成后的有效餐厅目录，而不是只展示仓库原始文件；
- 支持按全部、楼下和附近筛选；
- 每家餐厅至少展示名称、范围和文字位置；有数据时可展示菜系、参考人均、副范围步行时间和公开资料入口；
- 即使餐厅记录中存在 `recommended_dishes`，第一版页面也不展示；
- 忌口信息遵循第 2.4 节的上下文展示规则，不固定展示“无猪肉”一栏；
- 第一版不复制、热链、上传或展示餐厅与菜品图片，也不提供外部地图入口。

### 6.5 反馈入口

第一版暂缓反馈功能：

- 网页不显示反馈入口，不创建或接入腾讯问卷，也不为此引入后端；
- 普通用户仍可在本地整理待入库名称，但第一版不负责把这些名称自动提交给维护者；
- 后续若重新启用反馈，应重新确认账号归属、数据责任、填答权限和接入方式，不把旧问卷方案视为已批准契约。

## 7. 本地数据与 JSON

### 7.1 存储边界

- 共享默认目录来自随网站部署的 `data/restaurants.json`，每次加载自动采用当前部署版本；不能把首次访问时的整份目录永久冻结在本地状态中；
- 浏览器只保存个人可变数据：设置、忌口偏好、个人正式新增、个人隐藏的共享餐厅 ID、待入库名称和午餐历史；
- 个人叠加层只允许“新增”和“隐藏”，不允许在本地任意覆盖共享餐厅字段；共享事实修正通过仓库 PR 完成；
- 有效随机池由当前共享目录加个人正式新增，再排除个人隐藏项得到；待入库名称不属于有效随机池；
- 每台设备保存独立历史；
- 不提供账号、后端数据库或跨设备同步；
- 浏览器数据被清除时不保证恢复，用户通过导出文件自行备份。

### 7.2 文件类型

网页只向普通用户提供一种 UTF-8 JSON 文件：

- `lunch-picker-backup.json`：个人设置、忌口偏好、个人新增／隐藏、待入库名称和午餐历史的完整快照。

备份包含 `schema_version` 和导出时间，但不复制共享默认目录。仓库内的 `data/restaurants.json` 是维护者和 PR 使用的共享数据源，不是网页提供给普通用户的第二种导入导出格式。

### 7.3 导入语义

- 导入前校验 JSON 结构和必要字段；
- 校验通过后展示备份摘要和覆盖警告，要求用户确认；
- 导入只整体替换当前设备的个人设置、忌口偏好、个人叠加层、待入库名称和历史；共享默认目录仍使用网站当前部署版本；
- 第一版不实现自动合并、分类选择恢复、复杂迁移、云端备份或冲突解决；
- 校验或写入失败时保持原数据不变。

### 7.4 现有 v1 本地状态迁移

- 下一版个人状态写入新的 `lunch-picker.state.v2`，不直接覆盖或自动删除现有 `lunch-picker.state.v1`；
- 每次启动先加载当前部署版共享目录。没有有效 v2 状态但存在有效 v1 状态时，执行一次 v1 → v2 迁移；
- 迁移保留旧设置和全部历史记录及餐厅名称快照；旧默认 28 个餐厅 ID 视为旧共享目录，不复制为个人新增；
- v1 餐厅列表中不属于旧默认 28 个 ID 的额外记录转为个人正式新增；对旧共享餐厅字段的本地覆盖不继续保留；
- v1 没有对应数据的个人隐藏、待入库名称和忌口偏好初始化为空或默认值；
- 迁移结果全部校验通过后才写入 v2。失败时保留 v1 原始数据并明确报错，不静默生成空状态或丢弃历史；
- 成功迁移后以 v2 为后续读写来源；新版备份仍只包含第 7.1 节定义的个人数据。

## 8. 技术与公开边界

- 本项目维护为独立 Git 仓库，当前工作区根目录即仓库根目录；
- 使用原生 HTML、CSS 和 JavaScript；
- 移动端优先，同时支持桌面浏览器；
- 不引入前端框架、构建系统、后端服务或运行时依赖；
- 目标托管平台为 GitHub Pages；微信小程序仅保留为备选；
- 公开仓库只包含代码、默认设置和共享默认餐厅目录；
- 个人历史和导出的完整备份不得进入仓库或部署产物；
- 第一版不接入访问统计、用户追踪或实时餐厅 API；
- 第一版不托管餐厅或菜品图片，不集成地图 SDK、地图链接或地图 API；
- 第一版不提供反馈入口或反馈后端。

远端仓库与 GitHub Pages 已于 2026-08-27 经用户授权后发布；仓库公开边界仍按本文执行。

### 8.1 餐厅贡献与 PR

- 当前共享默认目录继续由 Git 管理，同事可以直接提交餐厅数据 PR；普通试用者可以在本地整理名称线索，并通过现有沟通渠道交给维护者；第一版网页不自动提交；
- 后续需要便于用户和同事共同维护公共餐厅目录的简单入口，可考虑腾讯文档或仓库内 CSV。具体方式尚未选定，本次不新增数据库、同步服务或第二份正式数据源；当前网页仍读取 `data/restaurants.json`；
- 名称线索由维护者交给 Agent 检索、核实并补全为正式记录，随后创建 PR，不能让静态网页自动抓取或自动入库；
- 餐厅数据 PR 使用 GitHub Actions 做最小自动校验：JSON 可解析、`id` 唯一、名称／范围／文字位置存在，以及副范围步行时间为 1–8 分钟；
- 自动校验通过不等于现实事实可信。每个 PR 仍需人工核实范围、位置、午间适用性和支持证据；
- 不自动合并 PR，也不允许机器人根据店名直接生成并发布未经审核的数据。

### 8.2 前端 Agent 协作

- 其他 Agent 可以接手界面设计，使用同一仓库的独立 Git worktree 和独立分支工作；
- 下一版保持最小三层：`js/core.mjs` 负责纯领域规则与数据契约，`js/app.mjs` 负责状态、持久化和业务 controller，新建 `js/view.mjs` 统一负责 DOM 渲染与事件绑定；
- 前端 Agent 可以修改 `index.html`、`styles.css`、`js/view.mjs` 和对应 UI 测试；不修改 `js/core.mjs`、`js/app.mjs` 或持久化测试；
- view 只向 controller 发送抽取、确认和保存记录等用户意图，并接收普通 view model；不得直接调用领域函数或读写 `localStorage`；
- controller／view 的选择器、事件和 view model 接口应集中定义并由最小契约测试保护，不依赖“只修改 `app.mjs` 的展示部分”这类隐式边界；
- 若界面方案需要改变随机规则、持久化语义、餐厅数据契约或 controller 接口，前端 Agent 必须停止并返回用户重新决策；
- 前端 Agent 完成本地提交后，由当前主 Agent 审查功能边界和测试，再按用户明确授权决定是否推送并创建 PR；
- GitHub 提交和 PR 的账号归属仍是实际认证用户，独立性来自 worktree、分支和审查边界，而不是虚构另一个 GitHub 身份。

## 9. 当前仍明确不做

- 实时抓取大众点评、高德或其他平台；
- 账号、登录、多人共享历史和跨设备同步；
- 动态评分推荐、菜品排行或价格加权；
- 正式共享餐厅字段的网页内编辑器；
- 日历、统计或社交分享；
- 自动备份、自动合并和复杂数据迁移；
- 推荐菜展示（`recommended_dishes` 可作为不展示的可选数据字段保留）；
- 餐厅与菜品图片、图片上传流程和地图入口；
- 网页反馈入口、腾讯问卷和反馈后端；
- 自动研究、自动入库或自动合并餐厅 PR；
- 微信小程序版本。

## 10. 验收标准

2026-09-23 用户决定先发布至现有 GitHub Pages，再自行在线测试。真实键盘、原生文件选择器及减少动态偏好保留为未完成验证项，不再作为本轮发布的前置阻塞，也不标记为已通过。

第一版完成时，应能在本地和 GitHub Pages 静态路径语义下验证：

1. 三种范围均只从正确候选池抽取；
2. 非冷却餐厅等概率，冷却权重默认 `0.0`；
3. `换一家` 在同一轮不重复；
4. `就吃这家`、手动记录、跳过、补记和修改能正确更新历史；
5. 周末与跳过记录不消耗两顿冷却；
6. 页面刷新后本地数据仍存在；
7. 手工记录支持候选选择和未关联自由文本；批量名称能按逗号或换行预览、去重并进入待入库，且不参与随机；
8. 忌口偏好不改变候选池，仅在相关时显示次要提示；
9. 网站采用仓库最新共享目录时，个人新增、隐藏、待入库、设置和历史仍被保留；
10. 单一完整备份 JSON 可以导出，并能按约定校验、摘要确认和整体恢复个人数据；
11. 个人历史不会出现在源码或默认部署数据中；
12. 页面不请求或展示餐厅／菜品图片，也不提供地图入口；
13. `recommended_dishes` 缺失或存在均不影响餐厅校验和随机池，页面不展示该字段；
14. 有效 v1 本地状态能按第 7.4 节迁移到 v2；迁移失败不会覆盖 v1 或静默丢失历史；
15. 餐厅数据 PR 的最小自动校验能拒绝结构错误、重复 ID、缺失最低字段和越界步行时间，但不会自动合并；
16. `js/view.mjs` 不调用领域函数或读写 `localStorage`，controller／view 接口有最小契约测试；
17. 主要流程可在手机窄屏和桌面宽屏使用；
18. 无网络后端仍可完成除外部公开资料跳转外的全部核心操作；
19. 说明页准确解释实际算法，并展示共享目录与个人叠加层合成后的有效餐厅名单及筛选结果。
