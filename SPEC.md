# 午餐随机选择器：产品与实现契约

状态：已确认

确认日期：2026-08-27

最近修订：2026-08-27（新增随机机制说明与餐厅目录）

本文档记录第一版已经确认的产品边界与验收口径。第一版以可用、透明、易维护为目标，不扩展为通用餐饮推荐平台。

## 1. 目标

为上海陆家嘴 L+MALL 泰康大厦的工作日午餐提供一个手机优先的随机选择工具，减少每天决定吃什么的成本。

典型使用时间为 11:30–13:00，默认服务 1–2 人的普通工作日午餐。

## 2. 餐厅范围

### 2.1 主范围

位于上海陆家嘴中心 L+MALL 内、从泰康大厦可以不离开综合体到达的全部合格餐饮门店。

### 2.2 副范围

从泰康大厦办公楼大堂出发，地图步行导航单程不超过 8 分钟、无需乘坐交通工具，且不属于主范围的合格餐饮门店。

### 2.3 入选条件

餐厅必须同时满足：

- 可以堂食，并能让一人点到一份完整正餐；
- 适合 1–2 人普通工作日午餐；
- 工作日午间通常无需预约；
- 到店后通常可在 40 分钟内完成用餐；
- 能提供至少一份不含猪肉的完整午餐；不要求清真餐厅或独立厨房；
- 位置、午间可用性和参考价格可以从公开来源合理核验。

纳入正餐餐厅、快餐、面馆、米饭套餐、轻食和美食广场档口。排除纯饮品店、甜品店、烘焙店、便利店、仅售小吃的门店和纯外卖档口。

正常午餐人均 100 元作为收录时的软上限。少量超过 100 元但仍适合单人午餐的门店可以入选；价格不影响抽取权重。

## 3. 初始白名单

- 使用 L+MALL 商户信息、大众点评、高德地图等公开来源交叉核验；
- 主范围目标约 20–30 家；
- 副范围目标约 8–12 家；
- 覆盖中餐、面饭、日料、韩餐、西式、轻食和快餐等常见类型；
- 追求代表性和可核验性，不追求穷尽全部门店；
- 实际符合条件的门店不足时不凑数，明显合适的门店较多时允许略微超过目标；
- 外部平台只用于建立和人工维护白名单，网页运行时不实时请求餐厅 API。

## 4. 随机规则

### 4.1 范围选择

提供三个选项：

- `楼下`：只抽取主范围，默认选择；
- `出去走走`：只抽取副范围；
- `都可以`：合并主、副范围。

### 4.2 权重与冷却

- 普通候选餐厅权重为 `1.0`；
- 吃过一家餐厅后，接下来的两条有效餐厅午餐记录中，该餐厅处于冷却状态；
- 冷却餐厅的权重由内部配置控制，第一版默认值为 `0.0`；
- 冷却权重不提供用户界面，但代码应允许以后修改；
- `跳过`、`未记录`、周末和其他非餐厅午餐不消耗冷却次数；
- 第一版不增加“最近若干顿最多出现若干次”等滚动频次规则；
- 在当前范围和历史约束确定权重后，按权重随机抽取。第一版中所有非冷却餐厅等概率。

### 4.3 一次抽取会话

- 抽中后显示 `就吃这家` 和 `换一家`；
- 只有点击 `就吃这家` 才写入实际午餐记录；
- 点击 `换一家` 后，从本轮尚未出现的可用餐厅中继续随机；
- 同一轮不会重复出现已经展示过的餐厅；
- 本轮候选耗尽时明确提示，不静默放宽范围或冷却规则。

## 5. 午餐记录

### 5.1 每日模型

- 每个工作日最多一条午餐记录；
- 用户可以补记过去的工作日，也可以修改或替换已有记录；
- 周末自动视为非工作日，不要求用户填写；
- 历史频次根据当前记录重新计算，编辑记录不会重复累计。

### 5.2 状态

- `restaurant`：已关联白名单餐厅，会更新该餐厅的历史；
- `skipped`：不关联餐厅、不进入冷却计数，可记录请假、公司聚餐在别处或其他原因；
- `unrecorded`：默认状态，表示尚未填写，不据此推断实际午餐。

公司聚餐若去了白名单餐厅，仍记录该餐厅并更新其 `last_seen`。公司聚餐地点不在白名单或不想记录时，使用 `skipped`。

每条餐厅记录应保留餐厅名称快照，避免餐厅以后从当前配置移除后历史无法显示。

## 6. 用户界面

### 6.1 首页

- 范围选择：`楼下`、`出去走走`、`都可以`；
- 主操作：开始随机；
- 结果卡片及 `就吃这家`、`换一家`；
- 今天已有记录时显示当前状态；
- 提供手动记录餐厅和 `今天跳过` 的入口。

### 6.2 结果卡片

只展示：

- 餐厅名称；
- 菜系或正餐类型；
- 商场楼层、铺位或街道地址；
- `楼下` 或 `出去走走` 标签；
- 副范围的参考步行时间；
- 参考人均价格；
- 外部地图链接。

结果卡片不搬运评分、评论、菜单图片或宣传照片。

### 6.3 历史与数据

- 历史页按日期倒序显示已有记录；
- 支持补记过去工作日和修改已有记录；
- 不做完整工作日日历、统计图表或趋势分析；
- 提供 JSON 导入导出入口；
- 不提供餐厅配置的网页内增删改界面。

### 6.4 说明与餐厅目录

- 公开解释范围过滤、两顿冷却、等概率抽取、换一家和确认记录的完整机制；
- 明确价格、菜系和距离不参与抽取权重；
- 展示当前浏览器正在使用的有效餐厅配置，而不是只展示仓库默认文件；
- 支持按全部、楼下和附近筛选；
- 每家餐厅展示名称、菜系、位置、参考人均、副范围步行时间、午餐建议、无猪肉提示、地图入口和公开资料入口；
- 午餐建议服务于本工具的时间和忌口场景，不宣称是平台销量或评分排行；
- 不直接复制或热链权属不明的点评、菜单或宣传照片；后续仅使用自有或已获授权的餐厅与菜品照片。

## 7. 本地数据与 JSON

### 7.1 存储边界

- 所有可变数据保存在当前浏览器本地；
- 每台设备保存独立历史；
- 不提供账号、后端数据库或跨设备同步；
- 浏览器数据被清除时不保证恢复，用户通过导出文件自行备份。

### 7.2 文件类型

提供两类 UTF-8 JSON 文件：

- `lunch-picker-backup.json`：餐厅配置、个人历史和设置的完整快照；
- `restaurants.json`：只包含餐厅配置，用于人工编辑、更新或分享。

两类文件均包含 `schema_version`。完整备份还包含导出时间。

### 7.3 导入语义

- 导入前校验 JSON 结构和必要字段；
- 校验通过后要求用户确认；
- 完整备份导入整体替换本地餐厅配置、历史和设置；
- 餐厅配置导入只整体替换当前餐厅配置，保留个人历史；
- 第一版不实现自动合并、复杂迁移、云端备份或冲突解决；
- 校验或写入失败时保持原数据不变。

## 8. 技术与公开边界

- 在当前工作区的 `lunch-picker` 子目录中维护独立 Git 仓库；
- 使用原生 HTML、CSS 和 JavaScript；
- 移动端优先，同时支持桌面浏览器；
- 不引入前端框架、构建系统、后端服务或运行时依赖；
- 目标托管平台为 GitHub Pages；微信小程序仅保留为备选；
- 公开仓库只包含代码、默认设置和初始餐厅白名单；
- 个人历史和导出的完整备份不得进入仓库或部署产物；
- 第一版不接入访问统计、用户追踪或实时餐厅 API。

远端仓库与 GitHub Pages 已于 2026-08-27 经用户授权后发布；仓库公开边界仍按本文执行。

## 9. 当前仍明确不做

- 实时抓取大众点评、高德或其他平台；
- 账号、登录、多人共享历史和跨设备同步；
- 动态评分推荐、菜品排行或价格加权；
- 餐厅配置编辑界面；
- 日历、统计或社交分享；
- 自动备份、自动合并和复杂数据迁移；
- 微信小程序版本。

## 10. 验收标准

第一版完成时，应能在本地和 GitHub Pages 静态路径语义下验证：

1. 三种范围均只从正确候选池抽取；
2. 非冷却餐厅等概率，冷却权重默认 `0.0`；
3. `换一家` 在同一轮不重复；
4. `就吃这家`、手动记录、跳过、补记和修改能正确更新历史；
5. 周末与跳过记录不消耗两顿冷却；
6. 页面刷新后本地数据仍存在；
7. 两种 JSON 可以导出，并能按约定校验、确认和恢复；
8. 个人历史不会出现在源码或默认部署数据中；
9. 主要流程可在手机窄屏和桌面宽屏使用；
10. 无网络后端仍可完成除外部地图和公开资料跳转外的全部核心操作；
11. 说明页准确解释实际算法，并展示当前浏览器的有效餐厅名单及筛选结果。
